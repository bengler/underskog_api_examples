# Sample Underskog API client app

Make sure the required gems are installed by running:

    bundle install

Then start a test server with:

    bundle exec unicorn

Then open http://localhost:8080/ in your browser.
